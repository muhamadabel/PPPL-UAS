package pages;

/**
 * Page konkret untuk assertion umum (judul/teks) yang dipakai lintas halaman.
 * Mewarisi isHeadingDisplayed()/isTextDisplayed() dari BasePage.
 */
public class CommonPage extends BasePage {
}
